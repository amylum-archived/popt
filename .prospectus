Prospectus.extra_dep('file', 'pkgforge')
Prospectus.extra_dep('file', 'prospectus_pkgforge')

item do
  expected do
    url_xpath
    url 'http://rpm5.org/files/popt/?C=M;O=D'
    xpath '//*[@id="autoindex"]/table/tr[4]/td[2]/a'
    regex /^popt-([\d.]+)\.tar\.gz$/
  end

  actual do
    git_tag
    regex /^(.*)-\d+$/
  end

  extend ProspectusPkgforge::DepItems
end
